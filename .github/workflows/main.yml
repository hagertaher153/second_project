on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:   
      - uses: actions/checkout@v3

      - name: "Login to Docker Hub"
        run: docker login --username hagertaher --password dckr_pat_8uxpPBSahNQnWS5EpX9DBfZlP0U
