on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Testing Githup Action'
        run: pwd   

      - name: 'Testing Printing'
        run: echo Hager